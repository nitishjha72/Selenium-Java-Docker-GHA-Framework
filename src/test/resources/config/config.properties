# url of the application
URL=https://d1uh9e7cu07ukd.cloudfront.net/selenium-docker/reservation-app/index.html

# Browser configuration: CHROME | FIREFOX | EDGE
BROWSER=CHROME

# Override existing reports: YES | NO
OVERRIDE_REPORTS=YES

# Capture screenshots for passed steps: YES | NO
PASSED_STEPS_SCREENSHOTS=YES

# Capture screenshots for failed steps: YES | NO
FAILED_STEPS_SCREENSHOTS=YES

# Capture screenshots for skipped steps: YES | NO
SKIPPED_STEPS_SCREENSHOTS=NO

# Retry failed tests: YES | NO
RETRY_FAILED_TESTS=YES

# Execution mode: LOCAL | REMOTE
EXECUTION_MODE=REMOTE

# Test selection strategy: GROUPS | ANNOTATION
TEST_SELECTION_STRATEGY=ANNOTATION

# Grid url for remote testing
GRID_URL=http://localhost:4444/wd/hub

# Test Suite to Run: SANITY | REGRESSION | SMOKE | ALL
TEST_CATEGORY=REGRESSION